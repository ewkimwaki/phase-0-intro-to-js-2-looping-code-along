// Code your solutions in this file
const writeCards = (names) => {
    return names.map((names) => `Thank you, ${names}, for the wonderful surprise gift!`);
}

const countDown = (num) => {
    while(num >= 0){
        console.log(num);
        num--;
    }
}